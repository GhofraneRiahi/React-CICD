name: Build, Test, Push Docker Image and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'            # React source code
      - 'Dockerfile'        # Dockerfile changes
      - 'package.json'      # npm package changes
      - 'package-lock.json' # npm lockfile changes
  workflow_dispatch:

jobs:
  build-test-push-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # needed to push to gh-pages branch

    steps:
      # 1. Checkout repo including Dockerfile and source code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Login to Docker Hub with secure secrets
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 3. Build Docker image (builds your app inside container as per Dockerfile)
      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/react-cicd:latest .

      # 4. Run tests inside the built Docker image container (adjust test command if needed)
      - name: Run tests inside Docker container
        run: docker run --rm ${{ secrets.DOCKER_USERNAME }}/react-cicd:latest 

      # 5. Push the Docker image to Docker Hub only if tests pass
      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/react-cicd:latest

      # 6. Create a container from the image to extract the built static files (/app/dist)
      - name: Create container to extract build output
        id: create_container
        run: |
          container_id=$(docker create ${{ secrets.DOCKER_USERNAME }}/react-cicd:latest)
          echo "container_id=$container_id" >> $GITHUB_OUTPUT

      # 7. Copy the static build files from container to the runner
      - name: Copy build files from container
        run: |
          mkdir -p dist
          docker cp ${{ steps.create_container.outputs.container_id }}:/app/dist ./dist

      # 8. Remove the temporary container used for copying files
      - name: Remove temporary container
        if: always()
        run: docker rm ${{ steps.create_container.outputs.container_id }} || true

      # 9. Deploy the extracted static site to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist